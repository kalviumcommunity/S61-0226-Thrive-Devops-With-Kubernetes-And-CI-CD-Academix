apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-uploads-pvc
spec:
  accessModes:
    - ReadWriteMany  # Multiple backend pods can write
  resources:
    requests:
      storage: 50Gi  # Adjust based on expected video storage needs
  storageClassName: standard  # Change to your cloud provider's storage class
